def decode_message( s: str, p: str) -> bool:

# write your code here
        for i= in range(len(p)):
            
            if p[i] == '*':
                continue
            elif p[i] == '?':
                continue
            elif p[i] == s[i]:
                continue
            elif :
                return False
            
  
        return True