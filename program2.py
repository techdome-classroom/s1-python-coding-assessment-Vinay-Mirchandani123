def decode_message( s: str, p: str) -> bool:

# write your code here
        for i in range(len(p)):
            
            if p[i] == '*':
                continue
            if p[i] == '?':
                continue
            
  
        return True